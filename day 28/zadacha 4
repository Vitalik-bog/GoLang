package main

import "fmt"

func main() {
    var n int
    fmt.Print("Введите число n: ")
    fmt.Scan(&n)
    
    sum := 0
    for i := 1; i <= n; i++ {
        sum += i
        //переменная +1
    }
    
    fmt.Printf("1+2+...+%d = %d\n", n, sum)
}

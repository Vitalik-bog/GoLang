package main

import "fmt"

func main() {
    num := 12345
    reversed := 0
    
    for num > 0 {
        digit := num % 10
        reversed = reversed*10 + digit
        num = num / 10
    }
    
    fmt.Printf("Перевернутое число: %d\n", reversed)
}

package main

import "fmt"

func main() {
    // 1. Объявляем и инициализируем пустой срез (динамический массив) целых чисел.
    // `var` - ключевое слово для объявления переменной
    // `numbers` - имя нашей переменной
    // `[]int` - тип "срез целых чисел"
    var numbers []int

    // 2. Добавляем элементы в конец среза с помощью встроенной функции `append`.
    // Функция возвращает новый срез, поэтому нам нужно перезаписать старую переменную.
    numbers = append(numbers, 10) // numbers теперь [10]
    numbers = append(numbers, 20) // numbers теперь [10, 20]
    numbers = append(numbers, 30) // numbers теперь [10, 20, 30]

    // 3. Меняем значение элемента по индексу 1 (второй элемент, т.к. отсчет с 0).
    numbers[1] = 25 // numbers теперь [10, 25, 30]

    // 4. Выводим весь срез на экран.
    fmt.Println("Весь срез:", numbers)

    // 5. Выводим отдельный элемент по индексу.
    fmt.Println("Второй элемент:", numbers[1])

    // 6. Выводим длину (количество элементов) среза.
    fmt.Println("Длина среза:", len(numbers))
}

package main

import "fmt"

// 1. Определяем структуру для узла списка.
// Узел хранит данные (value) и указатель на следующий узел (next).
type Node struct {
    value int
    next  *Node // Указатель на структуру такого же типа (Node)
}

// 2. Определяем структуру для самого списка.
// Пока она просто хранит указатель на начало (голову) списка.
type LinkedList struct {
    head *Node
}

func main() {
    // 3. Создаем экземпляр связанного списка.
    // Пока он пустой, поэтому head = nil.
    list := LinkedList{}
    fmt.Println("Создан пустой список:", list)
}

package main

import "fmt"

func main() {
    // 1. Создаем и заполняем срез строк. Делаем это в одну строку.
    fruits := []string{"Яблоко", "Банан", "Апельсин","Кокос"}

    // 2. Начинаем цикл `for` для обхода среза.
    // `i` - это текущий индекс (0, 1, 2...)
    // `fruit` - это значение fruits[i] на текущем шаге
    // `range fruits` - означает "пройти по диапазону среза fruits"
    for i, fruit := range fruits {
        // 3. На каждой итерации цикла выводим индекс и значение.
        fmt.Printf("Индекс: %d, Фрукт: %s\n", i, fruit)
    }

    // 4. Если индекс не нужен, можно использовать пустой идентификатор `_`.
    fmt.Println("\nТолько фрукты:")
    for _, fruit := range fruits {
        fmt.Println(fruit)
    }
}

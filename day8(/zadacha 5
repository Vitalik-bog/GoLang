package main

import "fmt"

func main() {
    // 1. Объявляем СТАТИЧЕСКИЙ массив фиксированного размера (3 элемента).
    // `[3]string` - тип "массив из 3 строк". Размер - часть типа данных.
    var fixedArray [3]string

    // 2. Заполняем массив по индексам.
    fixedArray[0] = "Первый"
    fixedArray[1] = "Второй"
    fixedArray[2] = "Третий"
    // fixedArray[3] = "Четвертый" // ЭТО ВЫЗОВЕТ ОШИБКУ КОМПИЛЯЦИИ! Выход за границы.

    // 3. Выводим массив.
    fmt.Println("Статический массив:", fixedArray)

    // 4. Можно создать и заполнить массив сразу.
    anotherArray := [2]int{100, 200} // Размер [2] задан явно
    fmt.Println("Другой массив:", anotherArray)
}

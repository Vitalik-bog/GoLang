package main

import "fmt"

func main() {
    // 1. Создаем статический массив - источник данных.
    sourceArray := [5]int{10, 20, 30, 40, 50}

    // 2. Создаем СРЕЗ, который смотрит на часть массива.
    // [1:4] означает "взять элементы с индекса 1 (включительно) до 4 (не включительно)".
    // Т.е. элементы sourceArray[1], sourceArray[2], sourceArray[3]
    var mySlice []int = sourceArray[1:4]

    // 3. Выводим результат.
    fmt.Println("Исходный массив:", sourceArray)
    fmt.Println("Срез [1:4]:", mySlice) // [20, 30, 40]

    // 4. Изменяем элемент в срезе.
    mySlice[0] = 999
    // 5. Так как срез - это всего лишь "окно" в исходный массив,
    // изменение через срез меняет и сам массив!
    fmt.Println("Массив ПОСЛЕ изменения среза:", sourceArray) // Элемент [1] стал 999
}

package main

import "fmt"

// 1. ОПРЕДЕЛЕНИЕ СТРУКТУРЫ
// Ключевое слово `type` объявляет новый тип.
// `Book` - имя нового типа.
// `struct` - говорит, что это структура.
// Внутри фигурных скобок перечисляем ПОЛЯ структуры и их типы.
type Book struct {
    Title  string
    Author string
    Year   int
}

// 2. ОБЪЯВЛЕНИЕ МЕТОДА
// func (b Book) - это "получатель" (receiver).
// `b` - это копия структуры Book, с которой мы работаем внутри метода.
// `Display()` - имя метода.
// Метод становится связанным с типом Book.
func (b Book) Display() {
    // Мы можем обращаться к полям получателя `b`
    fmt.Printf("Книга: '%s' by %s (%d год)\n", b.Title, b.Author, b.Year)
}

func main() {
    // 3. СОЗДАНИЕ ЭКЗЕМПЛЯРА СТРУКТУРЫ
    // Вариант 1: С указанием имен полей (лучше для читаемости)
    myBook := Book{
        Title:  "Война и мир",
        Author: "Лев Толстой",
        Year:   1869,
    }

    // Вариант 2: Без указания имен (важен порядок)
    // myBook := Book{"Война и мир", "Лев Толстой", 1869}

    // 4. ВЫЗОВ МЕТОДА
    // Метод вызывается через точку от переменной-экземпляра структуры.
    myBook.Display()
}

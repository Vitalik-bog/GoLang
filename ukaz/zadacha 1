package main

import "fmt"

func main() {
    a, b := 10, 20
    fmt.Printf("До: a=%d, b=%d\n", a, b)
    swap(&a, &b)
    fmt.Printf("После: a=%d, b=%d\n", a, b)
}

// Решение
func swap(x, y *int) {
    *x, *y = *y, *x
}

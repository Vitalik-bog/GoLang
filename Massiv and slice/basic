package main

import "fmt"

func main() {
   a := []int{10, 20, 30, 40}
   b := a[1:3] // b = [20, 30]
    b[0] = 999  // Меняем элемент в 'b'

fmt.Println(a) // [10, 999, 30, 40] - элемент в 'a' тоже изменился!
fmt.Println(b) // [999, 30]
}

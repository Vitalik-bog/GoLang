package main

import "fmt"

func main() {
    arr := [5]int{1, 2, 3, 4, 5}
    slice := arr[1:3] 

    fmt.Println("slice:", slice)           // [2, 3]
    fmt.Println("len(slice):", len(slice)) // 2
    fmt.Println("cap(slice):", cap(slice)) // 4
}
